#include "main.h"

/**
 * _abs - Compute the absolute value of an integer
 * @c: The character to be checked.
 *
 * Return: ALways 0.
 */
int_abs(int)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
