#include "main.h"

/**
 * times_table - pritns the 9 times tables
 * @n: as the integer
 * Return: Always 0.
 */
void times_table(void)
{
	if (n = 0; n <= 12; n++)
	int times_table = n * 9;
	return (times_table);
}
